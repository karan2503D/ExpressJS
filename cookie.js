const express = require("express");
const app = express();

// const cookie = require("cookie-parser")

// app.use(cookie())

// app.get('/',function(req,res){
//     res.cookie('name','Karan',{expires:10000+Date.now()}).send('cookie set');

// })

app.listen(3000);